#include "graphics.h"
#include "window.h"
#include "application.h"

int main(int argc, char *argv[])
{
  // const int width = 800;
  // const int height = 600;
  // const char *title = "fluid-engine";
  // Graphics::Window window{title, width, height};
  // window.render();
  // return 0;

  run();
}
